services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - DB_TYPE=sqlite
      - SECRET_KEY=production-query-secret-key-2024
    volumes:
      - ./production.db:/app/production.db
    restart: unless-stopped
